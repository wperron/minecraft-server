{
  "name": "scallywag-bot",
  "version": "0.0.6",
  "description": "Discord bot to invoke a Lambda function",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build": "npm install && zip -r package.zip ./*"
  },
  "author": "wperron",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.814.0",
    "discord.js": "^12.2.0"
  }
}
